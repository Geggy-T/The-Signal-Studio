// Deprecated. The cut SFX now ships as public/cut.mp3 and is loaded via
// staticFile("cut.mp3") in SignalClip.tsx (the render-safe path — data URIs are
// not reliably supported for <Audio> by @remotion/renderer). No longer imported.
export {};
